<%= fetch(:logrotate_log_path) %>/*.log {
    daily
    rotate <%= fetch(:logrotate_files_to_keep) %>

    missingok
    compress
    delaycompress
    notifempty
    copytruncate

    su <%= fetch(:user) %> <%= fetch(:user) %>
}
